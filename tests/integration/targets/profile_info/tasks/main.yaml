- name: account_info
  block:
    - name: Get info about the current profile
      linode.cloud.profile_info:
        api_token: '{{ api_token }}'
        ua_prefix: '{{ ua_prefix }}'
      register: profile

    - assert:
        that:
          - profile.profile.email | length > 0