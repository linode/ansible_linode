---
- hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - meta: refresh_inventory

    - debug:
        var: hostvars

    - name: Test inventory without filter
      assert:
        that:
          - "'ansible-test-inventory' in hostvars"